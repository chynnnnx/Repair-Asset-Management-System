@page "/add-room"
@inherits RoomManagementBase
@using Client.ViewModels

<link href="css/components/admin/RoomManagement.css" rel="stylesheet" />
<RoleGuard RequiredRole="Admin">
    <MudContainer MaxWidth="MaxWidth.ExtraExtraLarge" Class="pa-6">
        <MudGrid Spacing="4">
            <MudItem xs="12" md="3">
                <MudCard Elevation="4" Class="add-room-card">
                    <MudCardHeader Class="add-room-card-header">
                        <CardHeaderContent>
                            <MudText Typo="Typo.h6" Class="add-room-title">
                                <MudIcon Icon="@Icons.Material.Filled.Add" Class="mr-2 add-room-icon" Size="Size.Small" />
                                Add New Room
                            </MudText>
                        </CardHeaderContent>
                    </MudCardHeader>
                    <MudCardContent Class="pa-2">
                        <MudTextField @bind-Value="newRoom.RoomName"
                                      Label="Room Name"
                                      Variant="Variant.Outlined"
                                      Adornment="Adornment.Start"
                                      AdornmentIcon="@Icons.Material.Filled.MeetingRoom"
                                      Class="mb-3 room-input"
                                      Size="Size.Small" />

                        <MudTextField @bind-Value="newRoom.Location"
                                      Label="Location"
                                      Variant="Variant.Outlined"
                                      Class="mb-3 room-input"
                                      Size="Size.Small" />

                        <MudNumericField @bind-Value="newRoom.Capacity"
                                         Label="Capacity"
                                         Variant="Variant.Outlined"
                                         Class="mb-3 room-input"
                                         Size="Size.Small" />

                    </MudCardContent>
                    <MudCardActions Class="pa-2">
                        <MudButton OnClick="AddRoom"
                                   Variant="Variant.Filled"
                                   FullWidth="true"
                                   StartIcon="@Icons.Material.Filled.Add"
                                   Size="Size.Small"
                                   Loading="@isLoading"
                                   Disabled="@isLoading"
                                   Class="add-room-button">
                            @(isLoading ? "Adding..." : "Add Room")
                        </MudButton>
                    </MudCardActions>

                </MudCard>
            </MudItem>

            <MudItem xs="12" md="9">
                <DataCard TItem="RoomViewModel"
                          Title=@($"Room List ({rooms.Count} rooms)")
                          Icon="@Icons.Material.Filled.List"
                          Items="rooms"
                          SearchSelector="room => room.RoomName ?? string.Empty"
                          SelectedItemsChanged="OnSelectionChanged">
                    <Toolbar>
                        <MudButton Disabled="!selectedRooms.Any() || isLoading"
                                   OnClick="DeleteSelectedRooms"
                                   Color="Color.Error"
                                   Variant="Variant.Filled"
                                   StartIcon="@Icons.Material.Filled.Delete"
                                   Size="Size.Small"
                                   Loading="@isLoading">
                            @(isLoading ? "Deleting..." : $"Delete Selected ({selectedRooms.Count})")
                        </MudButton>
                    </Toolbar>


                    <Columns>
                        <SelectColumn T="RoomViewModel" />
                        <PropertyColumn T="RoomViewModel" TProperty="int" Property="x => x.RoomId" Title="Room ID">
                            <CellTemplate>
                                <MudChip T="string"
                                         Size="Size.Small"
                                         Variant="Variant.Text"
                                         Class="room-chip">
                                    #@context.Item.RoomId
                                </MudChip>
                            </CellTemplate>
                        </PropertyColumn>

                        <PropertyColumn T="RoomViewModel" TProperty="string" Property="x => x.RoomName" Title="Room Name">
                            <CellTemplate>
                                <div class="d-flex align-center">
                                    <MudIcon Icon="@Icons.Material.Filled.MeetingRoom"
                                             Size="Size.Small"
                                             Class="mr-2 room-icon" />
                                    <MudText>@context.Item.RoomName</MudText>
                                </div>
                            </CellTemplate>
                        </PropertyColumn>
                        <PropertyColumn T="RoomViewModel" TProperty="string" Property="x => x.Location" Title="Location">
                            <CellTemplate>
                                <div class="d-flex align-center">
                                  
                                    <MudText>@context.Item.Location</MudText>
                                </div>
                            </CellTemplate>
                        </PropertyColumn>
                        <PropertyColumn T="RoomViewModel" TProperty="int" Property="x => x.Capacity" Title="Capacity">
                            <CellTemplate>
                                <div class="d-flex align-center">
                                    
                                    <MudText>@context.Item.Capacity</MudText>
                                </div>
                            </CellTemplate>
                        </PropertyColumn>

                        <TemplateColumn T="RoomViewModel" Title="Edit">
                            <CellTemplate Context="cellContext">
                                <MudButton OnClick="@(() => Update(cellContext.Item))"
                                           Size="Size.Small"
                                           Variant="Variant.Filled"
                                           Class="edit-room-button">
                                    <MudIcon Icon="@Icons.Material.Filled.Edit" Size="Size.Small" />
                                </MudButton>
                            </CellTemplate>
                        </TemplateColumn>
                    </Columns>
                </DataCard>

            </MudItem>
        </MudGrid>
    </MudContainer>
</RoleGuard>
